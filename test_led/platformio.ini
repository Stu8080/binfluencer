; Binfluencer — LED cycle test (no WiFi / API)
; Cycles between GREEN and BLACK bins every 5 s.

[env:seeed_xiao_esp32s3]
platform      = espressif32
board         = seeed_xiao_esp32s3
framework     = arduino
monitor_speed = 115200
lib_deps =
    fastled/FastLED @ ~3.9.0
